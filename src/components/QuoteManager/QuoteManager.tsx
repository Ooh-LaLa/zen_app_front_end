// assets



// types
import { Profile } from '../../types/models'

interface QuoteManagerProps {
	profile: Profile
}

const QuoteManager = (props: QuoteManagerProps): JSX.Element => {
	const { profile } = props


  return (
    <section>
 
    </section>
  )
}

export default QuoteManager